# 拓扑可行性与验证状态

最后更新：2026-08-08

本文档记录 `topologies.TOPOLOGY_REGISTRY` 中每个拓扑的电气验证状态。拓扑已注册、能够生成网表或已被单元测试覆盖，并不等同于该电路在电气上有效。

## 状态定义

- **已验证**：真实 Spectre 标称仿真满足文档所列指标，并且要求的 PVT 扫描通过。该状态仅适用于证据中注明的 PDK、电压域、设计指标、负载和测试平台。
- **特定情况可行**：真实 Spectre 仿真证明电路在指定条件下表现出预期行为，但拓扑在这些条件之外会失败，或尚未完成完整的标称/PVT 资格验证。
- **未验证**：缺少完整的真实 Spectre 证据，或真实仿真已经暴露出阻塞问题。这并不表示其底层架构在理论上不可行。

所有状态变更都必须引用持久保存的 `results.json`；若要标记为 **已验证**，还必须提供真实的 `pvt/pvt_results.json`，且其中 `pvt_pass=true`。dry-run、生成网表、单元测试或原理图/版图证据均不得用于提升电气验证状态。

## 注册表汇总

| 拓扑 | 领域/架构 | 状态 | 当前资格验证或阻塞问题 |
|---|---|---|---|
| `5t_ota` | 单级五管 OTA | **特定情况可行** | TSMC28、0.9 V、TT 条件下，默认参数 AC 验证得到增益 24.38 dB、GBW 135.61 MHz、相位裕度 92.29°，关键工作点通过。尚无 PVT 资格验证。 |
| `two_stage_ota` | 两级 Miller OTA | **已验证** | TSMC28 证据表明，所引用的冒烟测试指标在标称条件下达标，并通过全部 27 个 `tt/ss/ff ×（vmin=0.9/vtyp=0.9/vmax=1.1 V）× -40/27/125°C` PVT 角。证据见下文。 |
| `pmos_input_two_stage_ota` | PMOS 输入两级 Miller OTA | **特定情况可行** | 修正内部 `vip`/`vin` 映射后，在 TSMC28、0.9 V、TT 条件下，默认参数 AC/SR/ST 仿真得到增益 67.01 dB、GBW 42.76 MHz、相位裕度 81.07°、最小转换速率 50.36 V/μs、稳定时间 25.05 ns。默认工作点下 `Mtail` 仍在线性区，且尚无 PVT 资格验证。 |
| `mzc_two_stage_ota` | Miller 零点消除/前馈两级 OTA | **特定情况可行** | TSMC28、0.9 V、TT 条件下，默认参数 AC 验证得到增益 42.39 dB、GBW 101.50 MHz、相位裕度 55.52°，关键工作点通过。尚无 PVT 资格验证。 |
| `pmos_input_mzc_two_stage_ota` | PMOS 输入 MZC 两级 OTA | **特定情况可行** | 修正主通路和前馈通路的输入映射后，在 TSMC28、0.9 V、TT 条件下，默认参数 AC/SR/ST 仿真得到增益 66.01 dB、GBW 42.80 MHz、相位裕度 78.84°、最小转换速率 51.20 V/μs、稳定时间 23.18 ns。默认工作点下 `Mtail` 和 `Mtailff` 仍在线性区，且尚无 PVT 资格验证。 |
| `folded_cascode` | 单级折叠共源共栅 OTA | **特定情况可行** | TSMC28、0.9 V、TT 条件下，默认参数 AC 验证得到增益 60.67 dB、GBW 133.11 MHz、相位裕度 80.89°。关键工作点通过，但若干器件接近饱和区边界。尚无 PVT 资格验证。 |
| `folded_cascode_two_stage` | 折叠共源共栅加第二级 OTA | **未验证** | 默认工作点增益为 103.40 dB、GBW 为 253.60 MHz，但相位裕度仅为 12.30°；电路在功能上不稳定或接近不稳定，完成资格验证前需要修复补偿。 |
| `nmcnr_three_stage` | 带消零电阻的嵌套 Miller 三级 OTA | **特定情况可行** | TSMC28、0.9 V、TT 条件下，默认参数 AC 验证得到增益 88.06 dB、GBW 27.18 MHz、相位裕度 71.65°，关键工作点通过。尚无 PVT 资格验证。 |
| `mnmc_three_stage` | 多通路嵌套 Miller 三级 OTA | **未验证** | 默认工作点能够收敛，增益为 69.76 dB、GBW 为 170.42 MHz，但相位裕度为 -94.52°。补偿/稳定性是当前阻塞问题。 |
| `nmcf_three_stage` | 嵌套 Miller/前馈三级 OTA | **未验证** | 默认工作点能够收敛，增益为 85.34 dB、GBW 为 184.18 MHz，但相位裕度为 -54.58°。补偿/稳定性是当前阻塞问题。 |
| `strongarm_latch` | 动态 StrongARM 锁存比较器 | **未验证** | 已有判决极性测试平台和物理实现适配器，但没有保留真实的标称/PVT 电气资格验证证据。 |
| `bandgap_ptat` | 层次化 PNP PTAT 基准 | **特定情况可行** | 在 `VDD=1.1 V`、TT 温度仿真中，PTAT 输出从 `-40°C 时 0.3849 V` 单调上升至 `125°C 时 0.6113 V`。在 `VDD=0.9 V` 时，低温 PNP 电压使 PMOS 镜像管的电压裕量不足，输出趋近电源电压。仍缺少完整的 1.1 V 启动、PSRR、线性调整率和 PVT 签核。 |
| `banba_sub1v_bandgap` | Banba 电流求和型亚 1 V 带隙基准 | **未验证** | 已知阻塞问题：在 `VDD=1.1 V` 时，自主启动电路仍处于导通状态，但核心收敛在接近零电流的状态（`Vref=0.342 mV`、`startup_success=false`）。在标称指标具备意义之前，必须先修复启动注入。 |
| `leung_mok_sub1v_bandgap` | Leung-Mok 2002 亚 1 V 带隙基准 | **未验证** | 已实现按论文映射的拓扑及专用测试平台；没有保留真实的标称/PVT 资格验证证据。 |
| `capless_ldo` | PMOS 调整管无片外电容 LDO | **未验证** | 已有专用环路、负载、PSR 和瞬态测试平台；没有保留真实的标称/PVT 资格验证证据。 |
| `dfc_capless_ldo` | DFC 无片外电容 LDO | **未验证** | 已实现按论文映射的拓扑及专用测试平台；没有保留真实的标称/PVT 资格验证证据。 |

## 运放单次验证

2026-08-08 保留的验证，对每个运放拓扑使用 `get_default_params()` 和该拓扑的主要 AC 测试平台，各执行一次真实 Spectre AC/DC 仿真。该验证不运行 BO、gm/Id 尺寸设计、SR/ST、Review 或 PVT。证据目录为：

`outputs/opamp_topology_default_validation_20260808_111005/`

本验证不使用 `main.py --max-iter 1`。在 gm/Id 模式下，`main.py` 可能依次运行 `DEFAULT_PARAMS` 基线、gm/Id 初始点和一次 BO 试验。优化器还会将初始候选加入队列，因此唯一一次 BO 试验通常只会重复初始点，而不会探索修复候选。

单次验证分类含义如下：

- `default_functional`（默认参数下功能正常）：Spectre 收敛；增益、GBW 和相位裕度均为有限正值；增益不低于 20 dB；相位裕度不低于 45°；关键工作点中没有处于线性区或状态未知的器件。
- `default_marginal`（默认参数下处于临界状态）：基本 AC 指标存在，但实际增益/相位裕度或关键工作点证据处于临界状态。
- `default_invalid`（默认参数下无效）：默认工作点缺少有效 AC 指标、相位裕度非正，或存在其他基本功能问题。这只是默认工作点的结果，并不能证明该架构永远无法通过尺寸优化实现有效设计。

| 拓扑 | 默认结果 | 增益 | GBW | 相位裕度 | 关键工作点 |
|---|---|---:|---:|---:|---|
| `5t_ota` | 功能正常 | `24.38 dB` | `135.61 MHz` | `92.29°` | 通过 |
| `two_stage_ota` | 功能正常 | `47.14 dB` | `98.87 MHz` | `58.82°` | 通过；`Mdiff2` 接近边界 |
| `pmos_input_two_stage_ota` | 临界 | `67.01 dB` | `42.76 MHz` | `81.07°` | 失败；默认尺寸下 `Mtail` 在线性区 |
| `mzc_two_stage_ota` | 功能正常 | `42.39 dB` | `101.50 MHz` | `55.52°` | 通过；`Mdiff2` 接近边界 |
| `pmos_input_mzc_two_stage_ota` | 临界 | `66.01 dB` | `42.80 MHz` | `78.84°` | 失败；默认尺寸下 `Mtail` 和 `Mtailff` 在线性区 |
| `folded_cascode` | 功能正常 | `60.67 dB` | `133.11 MHz` | `80.89°` | 通过；五个关键器件接近边界 |
| `folded_cascode_two_stage` | 临界 | `103.40 dB` | `253.60 MHz` | `12.30°` | 通过；五个关键器件接近边界 |
| `nmcnr_three_stage` | 功能正常 | `88.06 dB` | `27.18 MHz` | `71.65°` | 通过 |
| `mnmc_three_stage` | 无效 | `69.76 dB` | `170.42 MHz` | `-94.52°` | 通过；稳定性失败 |
| `nmcf_three_stage` | 无效 | `85.34 dB` | `184.18 MHz` | `-54.58°` | 通过；稳定性失败 |

上述 PMOS 输入拓扑在修正内部输入极性后重新运行，同时保留公开的 `(vip vin vout ibias vdd vss)` 端口约定。修正后的 AC/SR/ST 证据保存在：

`outputs/pmos_input_polarity_fix_validation_20260808_111753/`

| 拓扑 | 增益 | GBW | 相位裕度 | 最小转换速率 | 0.1% 稳定时间 |
|---|---:|---:|---:|---:|---:|
| `pmos_input_two_stage_ota` | `67.01 dB` | `42.76 MHz` | `81.07°` | `50.36 V/μs` | `25.05 ns` |
| `pmos_input_mzc_two_stage_ota` | `66.01 dB` | `42.80 MHz` | `78.84°` | `51.20 V/μs` | `23.18 ns` |

全部六项 Spectre 分析均以零错误完成。默认工作点剩余的关键工作点失败源于尺寸/电压裕量限制，而非反馈极性错误，因此这些拓扑仍归类为特定情况可行，而不是已完全验证。

## 已验证证据

### `two_stage_ota`

- PDK profile：`tsmc28`。
- 标称结果：`outputs/two_stage_ota_pvt_smoke/results.json`。
- 标称指标：增益 `45.87 dB`、GBW `34.88 MHz`、相位裕度 `67.60°`、功耗 `138.7 μW`、最小转换速率 `36.64 V/μs`、稳定时间 `19.25 ns`；`all_targets_met=true`。
- PVT 结果：`outputs/two_stage_ota_pvt_smoke/pvt/pvt_results.json`。
- PVT 覆盖：27/27 个角全部通过。保留的最差值为增益 `39.12 dB`、GBW `27.4 MHz`、相位裕度 `62.88°`、功耗 `0.14 mW`、转换速率 `34.27 V/μs`、稳定时间 `25.13 ns`。
- 适用范围：该证据验证的是所引用的设计指标和负载，并不表示拓扑元数据范围内的每组指标都可实现。

## 特定情况可行的证据

### `bandgap_ptat`

默认 `tsmc28`、`VDD=0.9 V` 仿真能够在室温下启动，但在完整温度范围内无效。在 `-40°C` 时，PNP 发射极节点和 `Vref` 接近电源电压，使 PMOS 镜像器件两端只剩几十毫伏压降。在约 `-4°C` 到 `-3°C` 之间会发生直流工作点跃迁。

将电源提高到 `1.1 V` 后，镜像器件的电压裕量恢复，并产生预期的单调 PTAT 响应：

| 温度 | 1.1 V 电源下的 Vref |
|---:|---:|
| `-40°C` | `0.3849 V` |
| `0°C` | `0.4394 V` |
| `27°C` | `0.4767 V` |
| `60°C` | `0.5224 V` |
| `125°C` | `0.6113 V` |

证据目录：`outputs/bandgap_ptat_validation_20260805_165934/`。根目录中的 `results.json` 记录 0.9 V 完整测试平台仿真；1.1 V 温度证据位于 `temperature_diagnostic_vdd_1p1/`。

提升为 **已验证** 前必须完成以下工作：

1. 明确定义 PTAT 输出、启动、功耗、PSRR 和线性调整率指标。
2. 在确定合格的电源电压下运行全部专用标称测试平台。
3. 在要求的工艺、电压和温度角下全部通过，且不存在镜像器件电压裕量或工作点阻塞问题。

## 已知阻塞问题

### `banba_sub1v_bandgap`

在 `VDD=1.1 V` 时，Spectre 能够收敛，但电路无法脱离启动状态：

- `Vref = 0.342 mV`、`startup_success=false`；
- `VRS = 0.3277 V`，`SUP` 保持在接近地电位，因此启动电路持续有效；
- `Va = 0.788 V`、`Vb = 1.080 V`、`Vg = 1.098 V`；
- 因此误差放大器会关断 P1/P2/P3 核心电流镜。

VREF 检测器和自动关断极性符合设计。将启动 LVT 器件替换为普通阈值器件并不能修复该问题。交换误差放大器输入会使 `Vref` 上升到电源电压，同样是无效状态。剩余的修复范围是启动注入机制，以及它与 Va/Vb 两支路不等阻抗之间的相互作用。

证据目录：`outputs/banba_sub1v_bandgap_validation_vdd_1p1_20260805_173123/`。

## 更新本文档

产生新证据时：

1. 确认 `pdk_profile_used.json` 与预期 PDK 和电压域一致。
2. 记录准确的设计指标、负载、电源、温度和测试平台集合。
3. 检查收敛情况、要求的指标、启动状态和关键工作点；仅仅收敛不能视为通过。
4. 只有预期工作域的一部分通过时，使用 **特定情况可行**。
5. 只有标称指标和真实 PVT 均通过后，才能使用 **已验证**。
6. 在更新且持久保存的结果证明阻塞问题已经解决之前，继续在说明中保留失败证据。
